{
  "name": "to_etsav",
  "version": "1.0.0",
  "description": "Public page of the ETSAV's project TO",
  "repository": "https://github.com/orzocogorzo/to_etsav",
  "license": "MIT",
  "dependencies": {
    "navigo": "^7.1.2"
  },
  "scripts": {
    "default": "export NODE_ENV='DEV' && gulp --gulpfile .devlite/gulpfile.js --cwd $PWD serve",
    "serve": "export NODE_ENV='DEV' && gulp --gulpfile .devlite/gulpfile.js --cwd $PWD serve",
    "build:pre": "export NODE_ENV='PRE' && gulp --gulpfile .devlite/gulpfile.js --cwd $PWD build",
    "build:pro": "export NODE_ENV='PRO' && gulp --gulpfile .devlite/gulpfile.js --cwd $PWD build"
  },
  "devDependencies": {
    "mustache": "^4.0.1"
  }
}
